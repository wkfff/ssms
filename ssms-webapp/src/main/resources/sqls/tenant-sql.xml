<?xml version="1.0" encoding="UTF-8" ?>

<sqlGroup name="tenant">
    <sqlGroup name="templateFolder">
        <sql id="getFolderByTemplateId">
            SELECT CONCAT('D-',CONVERT(SID,CHAR)) SID, C_NAME, CONCAT('D-',CONVERT(R_SID,CHAR)) R_SID
            FROM SSM_STDTMP_FOLDER
            WHERE R_TEMPLATE = ?
            UNION ALL
            SELECT CONCAT('F-',CONVERT(SID,CHAR)) SID, C_NAME, CONCAT('D-',CONVERT(R_SID,CHAR)) R_SID FROM SSM_STDTMP_FILE
            WHERE R_SID IN (
                SELECT SID FROM SSM_STDTMP_FOLDER
                WHERE R_TEMPLATE = ?
            )
        </sql>
        <sql id="getFolderByFolderTmp">SELECT * FROM SSM_STDTMP_FOLDER WHERE R_SOURCE=?</sql>
    </sqlGroup>
    <sqlGroup name="templateFile">
        <sql id="getFileByFileTmp">SELECT * FROM SSM_STDTMP_FILE WHERE R_SOURCE=?</sql>
    </sqlGroup>
    
    <sqlGroup name="grade">
        <sql id="getDataFromEnterprise">
            SELECT C_CATEGORY,C_PROJECT,C_REQUEST,C_CONTENT,N_SCORE,C_METHOD,C_DESC,N_SCORE_REAL FROM SSM_GRADE_E_D WHERE R_SID=?
        </sql>
        <sql id="insertToReview">insert into ssm_grade_r_d (R_SID,C_DESC,N_SCORE_REAL) values(?,?,?,?)</sql>
    </sqlGroup>
</sqlGroup>
